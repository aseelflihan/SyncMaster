{
  "name": "syncmaster",
  "version": "0.1.0",
  "private": true,
  "description": "AI Audio-Text Synchronization Platform – convenience wrapper for Streamlit dev server",
  "scripts": {
    "dev": "streamlit run app.py --server.port 8501 --server.address localhost"
  }
} 